import React from 'react';
import { toast } from "react-toastify";

const help = (props) => {
    
 const notify = () => toast.success("Wow so easy !");
    
    return (
      <div className="help">
       
      </div>
    );
}

export default help;